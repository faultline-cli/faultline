{"schema_version":"workflow.v1","mode":"agent","status":"matched","failure_id":"missing-executable","title":"Required executable or runtime binary missing","source":"stdin","context":{},"evidence":["exec /__e/node20/bin/node: no such file or directory"],"files":["Dockerfile",".github/workflows/ci.yml",".github/workflows/release.yml","scripts/cli-smoke.sh","scripts/docker-smoke.sh","scripts/release-build.sh","scripts/resolve-extra-pack.sh","scripts/smoke-release.sh"],"local_repro":["command -v \u003ctool\u003e","ls -l \u003cexpected-path\u003e"],"verify":["command -v \u003ctool\u003e"],"steps":["Confirm the top diagnosis `missing-executable` by reproducing the failure from the same command or CI step if possible.","Use the matched evidence lines as the starting point for triage: exec /__e/node20/bin/node: no such file or directory.","Inspect the most relevant local files first: Dockerfile, .github/workflows/ci.yml, .github/workflows/release.yml, scripts/cli-smoke.sh, scripts/docker-smoke.sh, scripts/release-build.sh, scripts/resolve-extra-pack.sh, scripts/smoke-release.sh.","Local repro: `command -v \u003ctool\u003e`.","Local repro: `ls -l \u003cexpected-path\u003e`.","Identify which command or runtime path the failing step expects to execute.","Confirm the binary exists in the active runner or container image:","Install the missing package in the job image or switch to an image that already contains the required runtime.","If the path is hard-coded, update the workflow or wrapper script to use the actual installed location.","If the failure happens only inside a containerized CI step, make sure the container image includes the same runtime that the host-based workflow expects.","Verify with `command -v \u003ctool\u003e` after the fix.","After the immediate fix, harden the workflow with: Standardize runner or container images for each workflow family."],"agent_prompt":"You are helping resolve a deterministic CI failure in the local repository.\nWorkflow schema: workflow.v1.\nTop diagnosis: missing-executable - Required executable or runtime binary missing.\nLog source: stdin.\nEvidence lines:\n- exec /__e/node20/bin/node: no such file or directory\nLikely files to inspect:\n- Dockerfile\n- .github/workflows/ci.yml\n- .github/workflows/release.yml\n- scripts/cli-smoke.sh\n- scripts/docker-smoke.sh\n- scripts/release-build.sh\n- scripts/resolve-extra-pack.sh\n- scripts/smoke-release.sh\nLocal repro commands:\n- command -v \u003ctool\u003e\n- ls -l \u003cexpected-path\u003e\nRecommended fix steps:\n- Identify which command or runtime path the failing step expects to execute.\n- Confirm the binary exists in the active runner or container image:\n- Install the missing package in the job image or switch to an image that already contains the required runtime.\n- If the path is hard-coded, update the workflow or wrapper script to use the actual installed location.\n- If the failure happens only inside a containerized CI step, make sure the container image includes the same runtime that the host-based workflow expects.\nVerification commands:\n- command -v \u003ctool\u003e\nWork deterministically: inspect the failing path, make the smallest complete change, and verify the exact failure is resolved.","artifact":{"schema_version":"failure_artifact.v1","status":"matched","fingerprint":"ed13830f","matched_playbook":{"id":"missing-executable","title":"Required executable or runtime binary missing","category":"build","severity":"high","detector":"log"},"evidence":["exec /__e/node20/bin/node: no such file or directory"],"confidence":0.84,"environment":{"source":"stdin","context":{},"pack_provenance":[{"name":"starter","playbook_count":101}]},"history_context":{"signature_hash":"4e9c13ba9d6a5ad10c761c94bcdf32b86abb858f75fc56874ec59c55b28bf11a"},"fix_steps":["Identify which command or runtime path the failing step expects to execute.","Confirm the binary exists in the active runner or container image:","Install the missing package in the job image or switch to an image that already contains the required runtime.","If the path is hard-coded, update the workflow or wrapper script to use the actual installed location.","If the failure happens only inside a containerized CI step, make sure the container image includes the same runtime that the host-based workflow expects."],"remediation":{"commands":[{"id":"local-repro-1","phase":"local-repro","command":["command","-v","\u003ctool\u003e"],"workdir":".","rationale":"Reproduce the failure before editing."},{"id":"local-repro-2","phase":"local-repro","command":["ls","-l","\u003cexpected-path\u003e"],"workdir":".","rationale":"Reproduce the failure before editing."},{"id":"verify-1","phase":"verify","command":["command","-v","\u003ctool\u003e"],"workdir":".","rationale":"Confirm the fix removes the failure signature."}],"patch_suggestions":[{"target_file":"Dockerfile","summary":"Adjust the likely failure surface for missing-executable.","actions":["Identify which command or runtime path the failing step expects to execute.","Confirm the binary exists in the active runner or container image:","Install the missing package in the job image or switch to an image that already contains the required runtime.","If the path is hard-coded, update the workflow or wrapper script to use the actual installed location.","If the failure happens only inside a containerized CI step, make sure the container image includes the same runtime that the host-based workflow expects."]},{"target_file":".github/workflows/*.yml","summary":"Adjust the likely failure surface for missing-executable.","actions":["Identify which command or runtime path the failing step expects to execute.","Confirm the binary exists in the active runner or container image:","Install the missing package in the job image or switch to an image that already contains the required runtime.","If the path is hard-coded, update the workflow or wrapper script to use the actual installed location.","If the failure happens only inside a containerized CI step, make sure the container image includes the same runtime that the host-based workflow expects."]},{"target_file":".github/workflows/*.yaml","summary":"Adjust the likely failure surface for missing-executable.","actions":["Identify which command or runtime path the failing step expects to execute.","Confirm the binary exists in the active runner or container image:","Install the missing package in the job image or switch to an image that already contains the required runtime.","If the path is hard-coded, update the workflow or wrapper script to use the actual installed location.","If the failure happens only inside a containerized CI step, make sure the container image includes the same runtime that the host-based workflow expects."]},{"target_file":".gitlab-ci.yml","summary":"Adjust the likely failure surface for missing-executable.","actions":["Identify which command or runtime path the failing step expects to execute.","Confirm the binary exists in the active runner or container image:","Install the missing package in the job image or switch to an image that already contains the required runtime.","If the path is hard-coded, update the workflow or wrapper script to use the actual installed location.","If the failure happens only inside a containerized CI step, make sure the container image includes the same runtime that the host-based workflow expects."]},{"target_file":"docker-compose*.yml","summary":"Adjust the likely failure surface for missing-executable.","actions":["Identify which command or runtime path the failing step expects to execute.","Confirm the binary exists in the active runner or container image:","Install the missing package in the job image or switch to an image that already contains the required runtime.","If the path is hard-coded, update the workflow or wrapper script to use the actual installed location.","If the failure happens only inside a containerized CI step, make sure the container image includes the same runtime that the host-based workflow expects."]}],"ci_config_diffs":[{"target_file":".github/workflows/*.yml","summary":"Update CI configuration to reflect the diagnosed remediation path.","before":["Failing step runs without the setup or path assumptions required by the diagnosis."],"after":["Add or reorder setup so the failing step sees the expected runtime, dependency, or artifact state.","Keep the CI change minimal and verify it with the shipped workflow.verify commands.","Identify which command or runtime path the failing step expects to execute."]},{"target_file":".github/workflows/*.yaml","summary":"Update CI configuration to reflect the diagnosed remediation path.","before":["Failing step runs without the setup or path assumptions required by the diagnosis."],"after":["Add or reorder setup so the failing step sees the expected runtime, dependency, or artifact state.","Keep the CI change minimal and verify it with the shipped workflow.verify commands.","Identify which command or runtime path the failing step expects to execute."]},{"target_file":".gitlab-ci.yml","summary":"Update CI configuration to reflect the diagnosed remediation path.","before":["Failing step runs without the setup or path assumptions required by the diagnosis."],"after":["Add or reorder setup so the failing step sees the expected runtime, dependency, or artifact state.","Keep the CI change minimal and verify it with the shipped workflow.verify commands.","Identify which command or runtime path the failing step expects to execute."]}]}},"remediation":{"commands":[{"id":"local-repro-1","phase":"local-repro","command":["command","-v","\u003ctool\u003e"],"workdir":".","rationale":"Reproduce the failure before editing."},{"id":"local-repro-2","phase":"local-repro","command":["ls","-l","\u003cexpected-path\u003e"],"workdir":".","rationale":"Reproduce the failure before editing."},{"id":"verify-1","phase":"verify","command":["command","-v","\u003ctool\u003e"],"workdir":".","rationale":"Confirm the fix removes the failure signature."}],"patch_suggestions":[{"target_file":"Dockerfile","summary":"Adjust the likely failure surface for missing-executable.","actions":["Identify which command or runtime path the failing step expects to execute.","Confirm the binary exists in the active runner or container image:","Install the missing package in the job image or switch to an image that already contains the required runtime.","If the path is hard-coded, update the workflow or wrapper script to use the actual installed location.","If the failure happens only inside a containerized CI step, make sure the container image includes the same runtime that the host-based workflow expects."]},{"target_file":".github/workflows/*.yml","summary":"Adjust the likely failure surface for missing-executable.","actions":["Identify which command or runtime path the failing step expects to execute.","Confirm the binary exists in the active runner or container image:","Install the missing package in the job image or switch to an image that already contains the required runtime.","If the path is hard-coded, update the workflow or wrapper script to use the actual installed location.","If the failure happens only inside a containerized CI step, make sure the container image includes the same runtime that the host-based workflow expects."]},{"target_file":".github/workflows/*.yaml","summary":"Adjust the likely failure surface for missing-executable.","actions":["Identify which command or runtime path the failing step expects to execute.","Confirm the binary exists in the active runner or container image:","Install the missing package in the job image or switch to an image that already contains the required runtime.","If the path is hard-coded, update the workflow or wrapper script to use the actual installed location.","If the failure happens only inside a containerized CI step, make sure the container image includes the same runtime that the host-based workflow expects."]},{"target_file":".gitlab-ci.yml","summary":"Adjust the likely failure surface for missing-executable.","actions":["Identify which command or runtime path the failing step expects to execute.","Confirm the binary exists in the active runner or container image:","Install the missing package in the job image or switch to an image that already contains the required runtime.","If the path is hard-coded, update the workflow or wrapper script to use the actual installed location.","If the failure happens only inside a containerized CI step, make sure the container image includes the same runtime that the host-based workflow expects."]},{"target_file":"docker-compose*.yml","summary":"Adjust the likely failure surface for missing-executable.","actions":["Identify which command or runtime path the failing step expects to execute.","Confirm the binary exists in the active runner or container image:","Install the missing package in the job image or switch to an image that already contains the required runtime.","If the path is hard-coded, update the workflow or wrapper script to use the actual installed location.","If the failure happens only inside a containerized CI step, make sure the container image includes the same runtime that the host-based workflow expects."]}],"ci_config_diffs":[{"target_file":".github/workflows/*.yml","summary":"Update CI configuration to reflect the diagnosed remediation path.","before":["Failing step runs without the setup or path assumptions required by the diagnosis."],"after":["Add or reorder setup so the failing step sees the expected runtime, dependency, or artifact state.","Keep the CI change minimal and verify it with the shipped workflow.verify commands.","Identify which command or runtime path the failing step expects to execute."]},{"target_file":".github/workflows/*.yaml","summary":"Update CI configuration to reflect the diagnosed remediation path.","before":["Failing step runs without the setup or path assumptions required by the diagnosis."],"after":["Add or reorder setup so the failing step sees the expected runtime, dependency, or artifact state.","Keep the CI change minimal and verify it with the shipped workflow.verify commands.","Identify which command or runtime path the failing step expects to execute."]},{"target_file":".gitlab-ci.yml","summary":"Update CI configuration to reflect the diagnosed remediation path.","before":["Failing step runs without the setup or path assumptions required by the diagnosis."],"after":["Add or reorder setup so the failing step sees the expected runtime, dependency, or artifact state.","Keep the CI change minimal and verify it with the shipped workflow.verify commands.","Identify which command or runtime path the failing step expects to execute."]}]}}
