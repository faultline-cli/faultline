name: ci
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Bash shell
        shell: bash
        run: |
          set -o pipefail
          if [[ -f package.json ]]; then
            npm test
          fi
