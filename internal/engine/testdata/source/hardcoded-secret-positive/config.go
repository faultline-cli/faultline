package config

const (
	// This is a test fixture for the hardcoded-secret source playbook.
	// The values below are intentionally fake and used only to validate pattern detection.
	APIKey     = "sk_live_FAKE_KEY_TEST_FIXTURE_ONLY"
	DBPassword = "hunter2-test-fixture"
)
