id: expired-credentials-near-miss
title: SSH key permission denied is a near-miss for expired credentials
fixture_class: minimal
path: internal/engine/testdata/fixtures/ssh-key-auth.log
expectation:
  expected_playbook: ssh-key-auth
  disallowed_playbooks:
    - expired-credentials