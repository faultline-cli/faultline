id: cargo-compile-error-canonical
title: Rust cargo build — type mismatch and unresolved import errors
fixture_class: minimal
normalized_log: |
  Run cargo build --release
    cargo build --release
    shell: /usr/bin/bash -e {0}
  Cloning repository done.

  $ rustup show
  Default host: x86_64-unknown-linux-gnu
  rustup home:  /home/runner/.rustup
  installed toolchain: stable-x86_64-unknown-linux-gnu
  active toolchain: stable-x86_64-unknown-linux-gnu (rust-toolchain.toml)

  $ cargo build --release
    Compiling myapp v0.1.0 (/home/runner/work/myapp/myapp)
  error[E0412]: cannot find type `Config` in this scope
   --> src/main.rs:12:17
    |
  12 |     let cfg: Config = Config::load()?;
    |                 ^^^^^^ not found in this scope
    |
  help: consider importing one of these items:
    |
  1 | use crate::config::Config;
    |

  error[E0308]: mismatched types
   --> src/main.rs:45:26
    |
  45 |     run_server(cfg, "8080")
    |                          ^^^^^ expected `u16`, found `&str`

  error: aborting due to 2 previous errors

  For more information about this error, try `rustc --explain E0412`.
  error: could not compile `myapp` due to 2 previous errors
expectation:
  expected_playbook: cargo-compile-error
