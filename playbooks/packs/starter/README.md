# Starter Pack

This directory is reserved for packaged starter-pack metadata and manifests.

The shipped starter playbooks currently live in `playbooks/bundled/`, which is
the default pack root loaded by Faultline.
